# Auth0 – use a Production application in production (dev keys are not for production).
# Create a separate Application in Auth0 Dashboard for production and set these there.
VITE_AUTH0_DOMAIN=your-tenant.auth0.com
VITE_AUTH0_CLIENT_ID=your-production-client-id
